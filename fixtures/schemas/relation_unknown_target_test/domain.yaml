version: 1
auth:
  scheme: none
http_backend: http://example.test
entities:
  Widget:
    id_field: id
    description: Minimal widget for loader relation-target validation
    fields:
      id:
        required: true
        value_ref: nv_widget_id
    relations:
      peer:
        description: Broken relation — target entity is not defined
        target: MissingEntity
        cardinality: one
capabilities:
  widget_get:
    description: Fetch widget
    kind: get
    entity: Widget
    provides:
    - id
values:
  nv_widget_id:
    type: string
    string_semantics: short
