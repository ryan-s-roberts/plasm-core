# Minimal CGS interchange for `plasm-plugin-stub` catalog metadata tests.
entry_id: stub_test
version: 1
http_backend: http://127.0.0.1:9
entities:
  X:
    name: X
    id_field: id
    fields:
      id:
        name: id
        field_type: string
        required: true
    relations: {}
capabilities:
  x_list:
    name: x_list
    kind: query
    domain: X
    mapping:
      template:
        method: GET
        path:
          - type: literal
            value: x
