# Upstream τ² retail tasks

The file `tau2_retail_tasks.json` is a verbatim copy of:

- Repository: [sierra-research/tau2-bench](https://github.com/sierra-research/tau2-bench)
- Path: `data/tau2/domains/retail/tasks.json`
- Branch: `main` (pinned at vendoring time; see git history for the fetch date)

Use the project’s SPDX / upstream license from that repository when redistributing. Regenerate `cases.yaml` with `python3 scripts/port_tau2_retail_tasks.py` after replacing this JSON.
