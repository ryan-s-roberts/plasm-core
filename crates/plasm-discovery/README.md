# plasm-discovery

Stepwise typed discovery over CGS catalogs: intent decomposition, phrase / lexical indexes, optional `fastembed` recall, graph-aware qualifier checks, and clarification gates (`AgentDiscovery`).

See repository docs for HTTP `/v1/discover-typed` and MCP `discover_capabilities` with `typed: true`.
