**Open or extend** one logical session. Pass stable **`intent`** and non-empty **`seeds`**: each `{api, entity}` (or `{entry_id, entity}`) is **one capability pick**. Federation rules: see MCP initialize workflow.

Returns **`logical_session_ref`** (`s0`, …) as the first Markdown line, then the **teaching TSV** (`e#` / `m#` / `p#` rows). Symbols are **session-local and append-only**; `p#` is scoped by catalog + entity + slot (same wire name on different entities gets distinct `p#`). Check latest TSV after append waves.

**Adding picks:** call again with **additional** `{api, entity}` rows when you discover more integrations.

**`_meta.plasm`:** may include `domain_revision`, `continuity.stale_binding_recovered`, `continuity.new_symbol_space`, and `catalog_entry_ids`.
